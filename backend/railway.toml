[build]
builder = "NIXPACKS"

[deploy]
startCommand = "mkdir -p uploads && python -m alembic upgrade head && python seed.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
